{
  "name": "multi-vendor-ecommerce",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix socket.io && npm install",
    "dev": "concurrently -n backend,socket,frontend -c blue,magenta,green \"npm run dev --prefix backend\" \"npm run dev --prefix socket.io\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:socket": "npm run dev --prefix socket.io",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
